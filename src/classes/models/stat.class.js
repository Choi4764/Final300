class Stat {
    constructor(level, hp, maxHp, mp, maxMp, atk, def, magic, speed, critical, critical_attack) {
      this.level = level;
      this.hp = hp;
      this.maxHp = maxHp;
      this.mp = mp;
      this.maxMp = maxMp;
      this.atk = atk;
      this.def = def;
      this.magic = magic;
      this.speed = speed;
      this.critical = critical;
      this.critical_attack =critical_attack;
    }
  }
  
  export default Stat;