export const config = {
    server: {
        port: PORT,
        host: HOST,
    },

    database: {
        database: DB_NAME,
        user: DB_USER,
        password: DB_PASSWORD,
        host: DB_HOST,
        port: DB_PORT,
    }
}